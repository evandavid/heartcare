<div data-role="page" data-add-back-btn="true">

  <div data-role="header" data-theme='e' >
   <a href="<%= url_for :action => :profile %>" class="ui-btn-left" data-icon="arrow-l" >
      Back
    </a>
    <h1>Update Password </h1>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :do_update_password %>">
      <% if @msg %>
          <div class="error-message"><%= @msg %></div>
      <% end %>

      <div data-role="fieldcontain">
        <input type="password" id="password" name="password" class="itemValue" placeholder="new password"/>
      </div>
      <fieldset class="ui-grid-a">
        <div class="ui-block-a"><button type="reset" >Reset</button></div>
        <div class="ui-block-b"><button type="submit" >Submit</a></div>
      </fieldset>
    </form>
  </div>

  <div data-position="fixed" data-role="footer" data-theme='e' >
      <h3>Heart Care</h3>
  </div>

</div>
